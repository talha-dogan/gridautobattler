/// <summary>
/// Defines the five equipment slots available on every player unit.
/// Each slot maps to a dedicated SpriteRenderer layer on CharacterEquipmentVisuals.
/// </summary>
public enum EquipmentSlot
{
    Helmet,
    Vest,
    Pants,
    Weapon,
    Shield
}
